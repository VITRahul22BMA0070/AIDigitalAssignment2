# AIDigitalAssignment2
Linear Regression is a supervised machine learning algorithm used to predict a continuous output variable based on one or more input features. It finds the best-fitting straight line (in case of simple linear regression) or hyperplane (for multiple variables).
